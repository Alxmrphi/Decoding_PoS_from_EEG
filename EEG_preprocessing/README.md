Example Jupyter notebook outlining preprocessing steps from raw EEG data. This consists of:

1) filtering to between [1 40] Hz
2) downsamplign (from 1 kHz to 250 Hz)
3) removing & interpolating noisy channels
